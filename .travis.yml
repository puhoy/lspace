dist: xenial   # required for Python >= 3.7

language: python

python:
 #- "3.5" # not working, tests failing on f-strings
 - "3.6"
 - "3.7"

install: "pip install -e .[dev]"

script: pytest

# Push the results back to codecov
after_success:
- codecov